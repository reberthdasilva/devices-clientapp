import styled from "styled-components";

const DeviceFilterStyled = styled.div``;

export const OptionTitleStyled = styled.span`
    font-weight: bold;
    padding: 0 10px;
`;

export default DeviceFilterStyled;
